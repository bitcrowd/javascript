// Use this file as a starting point for your project's .eslintrc.
// Copy this file, extend one of the shared configurations and
// add rule overrides as needed (but use overrides sparingly).
{
  "extends": "airbnb",
  "root": true
}
